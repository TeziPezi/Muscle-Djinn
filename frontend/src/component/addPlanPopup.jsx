import React, { useState, useEffect } from "react";
import axios from 'axios';
import '../styles.css';

const AddPlanPopup = ({show}) => {

    

    return (
        <div className={`popup ${show ? 'show': ''}`}>
            <div>
                blabla
                <button ></button>
            </div>
        </div>
    )
}

export default AddPlanPopup